spring:
  application: sistema-cadastro-api
  datasource:
    url: jdbc:mysql://localhost:3306/sistema_cadastro?createDatabaseIfNotExist=true
    username: root
    password: admin


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true


server:
  port: 9090
